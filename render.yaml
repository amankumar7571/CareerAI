services:
  - type: web
    name: career-guidance-backend
    env: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt && python ml_pipeline/train_model.py
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
